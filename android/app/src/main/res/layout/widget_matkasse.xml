<?xml version="1.0" encoding="utf-8"?>
<!-- Matkasse home-screen widget: serif title + remaining count, round Moss "+"
     (quick add without opening the app), and the live unchecked-items list. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg"
    android:paddingStart="16dp"
    android:paddingEnd="12dp"
    android:paddingTop="10dp"
    android:paddingBottom="8dp">

    <LinearLayout
        android:id="@+id/widget_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/widget_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_title_fallback"
                android:fontFamily="@font/manrope"
                android:textStyle="bold"
                android:textSize="20sp"
                android:textColor="@color/charcoal"
                android:maxLines="1"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/widget_count"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/manrope"
                android:textSize="13sp"
                android:textColor="@color/moss"
                android:maxLines="1" />
        </LinearLayout>

        <ImageButton
            android:id="@+id/widget_add"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="@drawable/widget_add_bg"
            android:src="@drawable/ic_widget_add"
            android:scaleType="center"
            android:contentDescription="@string/widget_quick_add" />
    </LinearLayout>

    <ListView
        android:id="@android:id/list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="4dp"
        android:divider="@drawable/widget_divider"
        android:dividerHeight="1dp"
        android:scrollbars="none" />

    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="4dp"
        android:gravity="center"
        android:text="@string/matkasse_empty"
        android:fontFamily="@font/manrope"
        android:textSize="13sp"
        android:textColor="@color/charcoal_muted" />
</LinearLayout>
