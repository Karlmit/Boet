<?xml version="1.0" encoding="utf-8"?>
<!-- Home-screen Matkasse widget: glanceable list + one-tap quick add.
     updatePeriodMillis is only a staleness backstop — real refreshes come from
     the Room items flow observed in BoetApp (see widget/MatkasseWidget.kt). -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="180dp"
    android:minResizeWidth="180dp"
    android:minResizeHeight="110dp"
    android:targetCellWidth="4"
    android:targetCellHeight="3"
    android:updatePeriodMillis="1800000"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:initialLayout="@layout/widget_matkasse"
    android:previewLayout="@layout/widget_matkasse"
    android:description="@string/widget_description" />
